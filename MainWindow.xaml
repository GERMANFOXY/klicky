<Window x:Class="Klicky.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Klicky"
        mc:Ignorable="d"
        Title="Klicky" SizeToContent="WidthAndHeight" MinHeight="420" MinWidth="640" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <Color x:Key="Primary">#E6B800</Color>
        <Color x:Key="PrimaryDark">#C59000</Color>
        <Color x:Key="Accent">#FFD871</Color>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#E6B800" />
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#C59000" />
        <SolidColorBrush x:Key="AccentBrush" Color="#FFD871" />
        <SolidColorBrush x:Key="CardBrush" Color="#0A0A0F" Opacity="0.78" />
        <SolidColorBrush x:Key="TextMain" Color="#F7F3FF" />
        <SolidColorBrush x:Key="TextSub" Color="#E8D79E" />
        <SolidColorBrush x:Key="ComboBackground" Color="#111118" />
        <SolidColorBrush x:Key="ComboHighlight" Color="#E6B800" />
        <SolidColorBrush x:Key="ComboForeground" Color="#F7F3FF" />
        <LinearGradientBrush x:Key="Backdrop" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#020304" Offset="0" />
            <GradientStop Color="#0A0A0F" Offset="0.45" />
            <GradientStop Color="#13141C" Offset="1" />
        </LinearGradientBrush>
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Margin" Value="0,4,0,12" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Foreground" Value="{StaticResource TextMain}" />
            <Setter Property="Background" Value="#1A1520" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="FontSize" Value="16" />
        </Style>
        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Margin" Value="0,8,0,0" />
            <Setter Property="Padding" Value="14 10" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource Primary}" Offset="0" />
                        <GradientStop Color="{StaticResource PrimaryDark}" Offset="1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="Chip" TargetType="Border">
            <Setter Property="Background" Value="#111118" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="10 8" />
            <Setter Property="BorderBrush" Value="#C59000" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,8,0,0" />
        </Style>
        <ControlTemplate x:Key="VioletComboToggle" TargetType="ToggleButton">
            <Border x:Name="Border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6">
                <Grid>
                    <Path x:Name="Arrow" Data="M 0 0 L 6 6 L 12 0 Z" Fill="{StaticResource TextMain}"
                          HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0" />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="{StaticResource ComboHighlight}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter TargetName="Border" Property="Opacity" Value="0.6" />
                    <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource TextSub}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        <Style x:Key="VioletComboBox" TargetType="ComboBox">
            <Setter Property="Margin" Value="0,4,0,12" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="{StaticResource TextMain}" />
            <Setter Property="Background" Value="{StaticResource ComboBackground}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="IsEditable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Template="{StaticResource VioletComboToggle}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False"
                                          HorizontalAlignment="Stretch"
                                          VerticalAlignment="Stretch"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}" />
                            <Border IsHitTestVisible="False" Margin="12,0,36,0" VerticalAlignment="Center">
                                <TextBlock x:Name="ContentSite"
                                           Text="{Binding SelectedItem.Content, RelativeSource={RelativeSource TemplatedParent}}"
                                           Foreground="{TemplateBinding Foreground}"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                            </Border>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="{StaticResource ComboBackground}" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1.5" CornerRadius="8">
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="32" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                <Setter TargetName="ToggleButton" Property="Background" Value="{StaticResource ComboHighlight}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Padding="26" CornerRadius="18" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1.5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <MediaElement x:Name="FireworksVideo" LoadedBehavior="Manual" UnloadedBehavior="Manual" Stretch="UniformToFill" IsHitTestVisible="False" Panel.ZIndex="-1" Opacity="0" />
                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Klicky Autoclicker" Foreground="{StaticResource TextMain}" FontSize="26" FontWeight="Bold" />
                                <TextBlock Text="Leichtgewichtig, schnell, schwarz-gold." Foreground="{StaticResource TextSub}" FontSize="14" Margin="0,4,0,14" />
                            </StackPanel>
                            <Button Grid.Column="1" Content="✕" FontSize="18" Foreground="{StaticResource TextMain}" Background="Transparent" BorderThickness="0" VerticalAlignment="Top" Margin="0,0,0,0" Click="CloseButton_Click" Cursor="Hand" />
                        </Grid>

                        <UniformGrid Columns="1" Margin="0,0,0,10">
                            <Border Style="{StaticResource Chip}" Background="#0F1728" BorderBrush="{StaticResource AccentBrush}">
                                <StackPanel>
                                    <TextBlock Text="Support" Foreground="{StaticResource AccentBrush}" FontSize="14" FontWeight="Bold" />
                                    <TextBlock Text="Powered by FOXYVERSE" Foreground="{StaticResource TextMain}" FontSize="12" Margin="0,4,0,0" />
                                    <TextBlock Text="Version 0.1.0.26.S" Foreground="{StaticResource TextSub}" FontSize="12" Margin="0,2,0,0" />
                                    <TextBlock Margin="0,6,0,0" FontSize="12">
                                        <Hyperlink NavigateUri="https://discord.gg/Hy38gjThfx" RequestNavigate="Hyperlink_RequestNavigate" Foreground="{StaticResource AccentBrush}">Support Discord</Hyperlink>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </UniformGrid>
                    </StackPanel>

                <Grid Grid.Row="1" Margin="0,4,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,12,0" VerticalAlignment="Top">
                        <TextBlock Text="Clicks per Second" Foreground="{StaticResource TextSub}" Margin="0,0,0,2" />
                        <TextBox x:Name="CpsInput" Style="{StaticResource InputBox}" Text="10" ToolTip="Wie viele Klicks pro Sekunde" TextChanged="CpsInput_TextChanged" />

                        <TextBlock Text="Intervall (ms, optional)" Foreground="{StaticResource TextSub}" Margin="0,0,0,2" />
                        <TextBox x:Name="IntervalInput" Style="{StaticResource InputBox}" Text="100" ToolTip="Millisekunden zwischen Klicks; überschreibt CPS wenn angegeben" />

                        <TextBlock Text="Maus-Button" Foreground="{StaticResource TextSub}" Margin="0,0,0,2" />
                        <ComboBox x:Name="ButtonSelect" Style="{StaticResource VioletComboBox}" SelectedIndex="0">
                            <ComboBox.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#1A1520" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#1A1520" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#F7F3FF" />
                                <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#F7F3FF" />
                            </ComboBox.Resources>
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Foreground" Value="{StaticResource TextMain}" />
                                    <Setter Property="Background" Value="{StaticResource ComboBackground}" />
                                    <Setter Property="Padding" Value="8 6" />
                                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource ComboHighlight}" />
                                            <Setter Property="Foreground" Value="{StaticResource ComboForeground}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource ComboHighlight}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                            <ComboBoxItem Content="Links" />
                            <ComboBoxItem Content="Rechts" />
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Top">
                        <Border Style="{StaticResource Chip}">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Ellipse Width="12" Height="12" Fill="{StaticResource AccentBrush}" Margin="0,0,8,0" />
                                <StackPanel>
                                    <TextBlock Text="Hotkey" Foreground="{StaticResource TextSub}" FontSize="12" />
                                    <TextBlock x:Name="HotkeyText" Text="F6 zum Start/Stop" Foreground="{StaticResource TextMain}" FontSize="14" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Chip}">
                            <StackPanel>
                                <TextBlock Text="Status" Foreground="{StaticResource TextSub}" FontSize="12" />
                                <TextBlock x:Name="StatusText" Text="Bereit" Foreground="{StaticResource TextMain}" FontSize="16" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="2" Margin="0,4,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Button x:Name="StartStopButton" Style="{StaticResource AccentButton}" Width="180" Click="StartStopButton_Click">Starten</Button>
                        <CheckBox x:Name="FollowCursorCheck" Content="Cursor folgen" Foreground="{StaticResource TextMain}" FontSize="13" VerticalAlignment="Center" Margin="16,0,0,0" IsChecked="True" />
                    </StackPanel>
                    <CheckBox x:Name="HoldModeCheck" Content="Halten-Modus (Roblox)" Foreground="{StaticResource TextMain}" FontSize="13" Margin="0,10,0,8" IsChecked="False" Checked="HoldModeCheck_Changed" Unchecked="HoldModeCheck_Changed" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="HoldDurationLabel" Text="Halte-Dauer (Sekunden):" Foreground="{StaticResource TextSub}" FontSize="14" VerticalAlignment="Center" Margin="0,0,12,0" Visibility="Collapsed" />
                        <TextBox x:Name="HoldDurationInput" Style="{StaticResource InputBox}" Text="3" Visibility="Collapsed" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,12,0,0">
                        <TextBlock Text="Hotkey:" Foreground="{StaticResource TextMain}" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0" />
                        <ComboBox x:Name="HotkeyComboBox" Style="{StaticResource VioletComboBox}" Width="100" SelectedIndex="5" SelectionChanged="HotkeyComboBox_SelectionChanged">
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Foreground" Value="{StaticResource TextMain}" />
                                    <Setter Property="Background" Value="{StaticResource ComboBackground}" />
                                    <Setter Property="Padding" Value="8 6" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource ComboHighlight}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource ComboHighlight}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                            <ComboBoxItem Content="F1" />
                            <ComboBoxItem Content="F2" />
                            <ComboBoxItem Content="F3" />
                            <ComboBoxItem Content="F4" />
                            <ComboBoxItem Content="F5" />
                            <ComboBoxItem Content="F6" />
                            <ComboBoxItem Content="F7" />
                            <ComboBoxItem Content="F8" />
                            <ComboBoxItem Content="F9" />
                            <ComboBoxItem Content="F10" />
                            <ComboBoxItem Content="F11" />
                            <ComboBoxItem Content="F12" />
                        </ComboBox>
                    </StackPanel>
                </StackPanel>

                <Grid Grid.Row="3" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock Text="Tip:" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" />
                        <TextBlock Text="Hotkey startet/stoppt. Einstellungen speichern automatisch." Foreground="{StaticResource TextSub}" Margin="8,0,0,0" />
                    </StackPanel>
                    <Button x:Name="CheckUpdateButton" Grid.Column="1" Style="{StaticResource AccentButton}" Padding="12 8" Width="170" Click="CheckUpdateButton_Click">Update prüfen</Button>
                </Grid>

                <!-- Fireworks overlay -->
                <Canvas x:Name="FireworksCanvas" Grid.Row="0" Grid.RowSpan="5" IsHitTestVisible="False" Panel.ZIndex="8" Opacity="0" Visibility="Collapsed" />
        </Grid>
    </Border>
</Window>
